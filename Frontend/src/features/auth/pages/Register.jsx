import { useState } from "react";
import { Link, useNavigate, Navigate } from "react-router";
import AuthLayout from "../components/AuthLayout";
import GoogleSignInButton from "../components/GoogleSignInButton";
import { Field, TextInput } from "../../../shared/components/Field";
import Button from "../../../shared/components/Button";
import Callout from "../../../shared/components/Callout";
import PageLoader from "../../../shared/components/PageLoader";
import { useAuth } from "../hooks/useAuth";
import "../auth.form.scss";
const EMAIL_REGEX=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;
function computePasswordStrength(pass){if(!pass)return{label:"None",tone:"faint",percent:0};let s=0;if(pass.length>=8)s++;if(pass.length>=12)s++;if(/[0-9]/.test(pass))s++;if(/[a-z]/.test(pass)&&/[A-Z]/.test(pass))s++;if(/[^A-Za-z0-9]/.test(pass))s++;if(s<=1)return{label:"Weak",tone:"error",percent:25};if(s===2)return{label:"Fair",tone:"warning",percent:50};if(s<5)return{label:"Strong",tone:"accent",percent:75};return{label:"Exceptional",tone:"success",percent:100}}
const Register=()=>{const navigate=useNavigate();const{user,loading,handleRegister,handleGoogleAuth}=useAuth();const[username,setUsername]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[confirmPassword,setConfirmPassword]=useState("");const[error,setError]=useState("");const[emailError,setEmailError]=useState("");const[submitting,setSubmitting]=useState(false);if(!loading&&user)return <Navigate to="/analyze/ats-score" replace/>;const strength=computePasswordStrength(password);const matches=password.length>0&&confirmPassword.length>0&&password===confirmPassword;const submit=async(e)=>{e.preventDefault();setError("");if(username.trim().length<3)return setError("Username must be at least 3 characters");if(!EMAIL_REGEX.test(email.trim()))return setEmailError("Please enter a valid email address");if(password.length<8)return setError("Password must be at least 8 characters");if(password!==confirmPassword)return setError("Passwords do not match");setSubmitting(true);try{await handleRegister({username:username.trim(),email:email.trim(),password});localStorage.setItem("ra_pending_verification_email",email.trim().toLowerCase());navigate("/verify-email")}catch(err){setError(err.response?.data?.message||err.message||"Registration failed.")}finally{setSubmitting(false)}};const google=async(credential)=>{setError("");try{await handleGoogleAuth(credential);navigate("/analyze/ats-score")}catch(err){setError(err.response?.data?.message||err.message||"Google sign-in failed.")}};if(loading)return <PageLoader label="Configuring your workspace"/>;return <AuthLayout eyebrow="Get started" title="Create your account">{error&&<Callout tone="error">{error}</Callout>}<GoogleSignInButton onCredential={google} disabled={submitting}/><div className="auth-form__divider"><span>or register with email</span></div><form className="auth-form" onSubmit={submit} noValidate><Field label="Username" htmlFor="username" hint="Minimum 3 characters" required><TextInput id="username" autoComplete="username" placeholder="johndoe" value={username} onChange={e=>setUsername(e.target.value)} required disabled={submitting}/></Field><Field label="Email address" htmlFor="email" error={emailError} required><TextInput id="email" type="email" autoComplete="email" placeholder="you@domain.com" value={email} onChange={e=>{setEmail(e.target.value);setEmailError("")}} required disabled={submitting}/></Field><Field label="Password" htmlFor="password" required><TextInput id="password" type="password" autoComplete="new-password" placeholder="At least 8 characters" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} disabled={submitting}/>{password&&<div className="auth-form__strength"><div className="auth-form__strength-header"><span className="auth-form__strength-label">Password Strength:</span><span className={`auth-form__strength-value auth-form__strength-value--${strength.tone}`}>{strength.label}</span></div><div className="auth-form__strength-bar"><div className={`auth-form__strength-fill auth-form__strength-fill--${strength.tone}`} style={{width:`${strength.percent}%`}}/></div></div>}</Field><Field label="Confirm password" htmlFor="confirmPassword" error={confirmPassword&&!matches?"Passwords do not match":null}><TextInput id="confirmPassword" type="password" autoComplete="new-password" placeholder="Re-enter your password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} disabled={submitting}/></Field><Button type="submit" size="lg" variant="primary" loading={submitting} disabled={submitting||username.trim().length<3||!email.trim()||password.length<8||password!==confirmPassword} className="auth-form__submit">{submitting?"Creating workspace…":"Create Account"}</Button></form><p className="auth-form__switch">Already have an account? <Link to="/login">Sign in</Link></p></AuthLayout>};export default Register;
